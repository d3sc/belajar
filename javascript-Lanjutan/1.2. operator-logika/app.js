// * operator logika
// ! Operator logika terdiri dari &&, ||, !
// ( && ) dibaca and
// ( || ) dibaca or
// dan terakhir ( ! ) dibaca Not

let x = 6;

// TODO: kalau menggunakan &&, dua duanya harus true agar menghasilkan nilai true. kalau hanya salah satunya saja false maka hasilnya juga akan false
// console.log(x % 2 === 0 && x <= 5);

// TODO: kalau menggunakan ||, jika salah satunya true maka akan menghasilkan nilai true.
// console.log(x % 2 === 0 || x <= 4);

// TODO: Operator ! untuk membalikan boolean
console.log(!(x < 4));
