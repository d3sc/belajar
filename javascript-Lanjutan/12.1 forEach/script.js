// forEach
// forEach adalah pengulangan khusus array, dan penulisan forEach nya harus benar, seperti E nya harus besar.
let list = ["ikbar", "tipang", "kulips"];

list.forEach((i) => console.log(`nama saya ${[i]}`));
